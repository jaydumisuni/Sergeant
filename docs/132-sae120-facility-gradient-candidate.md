# SAE-120 — GitHub / Local Facility Gradient

Status: **CANDIDATE**.

Construction base: `1ba0fbbafc9423ed9b098137e0669fe941b052d0` — canonical SAE-110 PROVEN closeout merge.

SAE-120 introduces a successor-only facility contract that preserves one exact ACR/Judge/Rust/PASS-law identity across unequal execution facilities. GitHub is permanently constrained/read-only. Local and IDE facilities may expose richer owner-approved working-tree, symbol, runtime/test, formal-tool, experiment, or draft-remediation capabilities, but facility richness grants no verdict-semantic authority.

Missing required facility or capability returns `UNKNOWN` with `can_support_pass=false`; it never degrades into a weaker PASS. Local/IDE writes require an explicit owner approval bit and cannot float independently of an enabled write facility. Facility rosters with mismatched ACR/Judge/Rust/PASS-law authority fail closed.

Hostile qualification in `tests/test_facility_gradient.py` covers GitHub write attempts, unapproved local writes, assurance-law substitution, missing required facilities/capabilities, and richer-local attempts to alter verdict meaning.

Candidate merge, if later authorized, grants no authority. `QUALIFIED_PRODUCT_CAPABILITY_GRADIENT` requires a separate exact lifecycle closeout. Genesis remains inactive and no normal Sergeant verdict authority is created here.
