# SAE-130B Semantic Coupling Candidate

Candidate implementation for the roadmap SAE-130B programme. It models producer-to-consumer, changed-config-to-unchanged-consumer, generated-artifact, persistence, and observer coupling as an explicit bounded graph and derives a deterministic semantic blast radius. Cycles terminate by visited-set closure; duplicate, self, or non-canonical edges fail closed.

This generation is CANDIDATE only. Candidate merge grants no qualified capability, normal verdict authority, Genesis activation, or downstream PROVEN state. `QUALIFIED_SEMANTIC_COUPLING_CAPABILITY` requires a separate qualification/lifecycle closeout with exact candidate, merge, proof, and independent-review identities.
