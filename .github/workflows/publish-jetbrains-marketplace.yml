name: Publish JetBrains Plugin

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing GitHub release tag to verify and receive assets
        required: false
        type: string
  release:
    types: [published]

permissions:
  contents: write

concurrency:
  group: jetbrains-marketplace-publish
  cancel-in-progress: false

jobs:
  publish:
    name: Build and publish Sergeant JetBrains preview
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.0.0"

      - name: Read plugin and release metadata
        id: metadata
        shell: bash
        env:
          RELEASE_EVENT_TAG: ${{ github.event.release.tag_name }}
          DISPATCH_TAG: ${{ inputs.release_tag }}
        run: |
          VERSION=$(grep '^pluginVersion=' adapters/jetbrains/gradle.properties | cut -d= -f2-)
          RELEASE_VERSION="${VERSION%-preview}"
          RELEASE_TAG="${RELEASE_EVENT_TAG:-$DISPATCH_TAG}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "release_version=$RELEASE_VERSION" >> "$GITHUB_OUTPUT"
          echo "release_tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"

      - name: Verify release tag matches plugin release line
        if: steps.metadata.outputs.release_tag != ''
        shell: bash
        env:
          RELEASE_TAG: ${{ steps.metadata.outputs.release_tag }}
          PLUGIN_RELEASE_VERSION: ${{ steps.metadata.outputs.release_version }}
        run: |
          TAG_VERSION="${RELEASE_TAG#v}"
          test "$TAG_VERSION" = "$PLUGIN_RELEASE_VERSION" || {
            echo "Release tag '$RELEASE_TAG' does not match JetBrains plugin release line '$PLUGIN_RELEASE_VERSION'."
            exit 1
          }

      - name: Verify JetBrains publishing token exists
        shell: bash
        env:
          JETBRAINS_MARKETPLACE_TOKEN: ${{ secrets.JETBRAINS_MARKETPLACE_TOKEN }}
        run: |
          test -n "$JETBRAINS_MARKETPLACE_TOKEN" || {
            echo "Repository secret JETBRAINS_MARKETPLACE_TOKEN is missing."
            exit 1
          }

      - name: Build JetBrains plugin
        run: gradle -p adapters/jetbrains clean buildPlugin

      - name: Verify plugin package and create checksum
        id: package
        shell: bash
        run: |
          PACKAGE=$(find adapters/jetbrains/build/distributions -maxdepth 1 -type f -name '*.zip' -print -quit)
          test -n "$PACKAGE" || { echo "JetBrains plugin ZIP was not produced."; exit 1; }
          sha256sum "$PACKAGE" > "$PACKAGE.sha256"
          echo "path=$PACKAGE" >> "$GITHUB_OUTPUT"
          echo "checksum=$PACKAGE.sha256" >> "$GITHUB_OUTPUT"
          echo "name=$(basename "$PACKAGE")" >> "$GITHUB_OUTPUT"

      - name: Upload plugin package as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: sergeant-${{ steps.metadata.outputs.version }}-jetbrains
          path: |
            ${{ steps.package.outputs.path }}
            ${{ steps.package.outputs.checksum }}
          if-no-files-found: error

      - name: Publish or confirm preview on JetBrains Marketplace
        shell: bash
        env:
          JETBRAINS_MARKETPLACE_TOKEN: ${{ secrets.JETBRAINS_MARKETPLACE_TOKEN }}
          PLUGIN_VERSION: ${{ steps.metadata.outputs.version }}
        run: |
          set +e
          OUTPUT=$(gradle -p adapters/jetbrains publishPlugin --stacktrace 2>&1)
          STATUS=$?
          set -e
          printf '%s\n' "$OUTPUT"
          if [ "$STATUS" -eq 0 ]; then
            exit 0
          fi
          printf '%s' "$OUTPUT" | grep -Fqi "already contains version $PLUGIN_VERSION in channel preview" && exit 0
          exit "$STATUS"

      - name: Attach JetBrains ZIP and checksum to GitHub Release
        if: steps.metadata.outputs.release_tag != ''
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ steps.metadata.outputs.release_tag }}
        run: gh release upload "$RELEASE_TAG" "${{ steps.package.outputs.path }}" "${{ steps.package.outputs.checksum }}" --clobber
