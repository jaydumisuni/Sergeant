name: Temporary Wire Transfer 14

on:
  push:
    branches:
      - train/model-free-core-campaign-2
    paths:
      - .github/workflows/temp-wire-transfer14.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: train/model-free-core-campaign-2
          fetch-depth: 0

      - name: Wire transfer 14 into canonical status review
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path("main_review/static_status_review.py")
          text = path.read_text(encoding="utf-8")

          replacements = (
              (
                  "from .static_transfer_13_review import run_static_transfer_13_review\n",
                  "from .static_transfer_13_review import run_static_transfer_13_review\n"
                  "from .static_transfer_14_review import run_static_transfer_14_review\n",
              ),
              (
                  "    transfer_13 = run_static_transfer_13_review(root_path, changed)\n"
                  "    selector_continuity = run_static_selector_continuity_review(root_path, changed)\n",
                  "    transfer_13 = run_static_transfer_13_review(root_path, changed)\n"
                  "    transfer_14 = run_static_transfer_14_review(root_path, changed)\n"
                  "    selector_continuity = run_static_selector_continuity_review(root_path, changed)\n",
              ),
              (
                  "        transfer_13,\n"
                  "        selector_continuity,\n",
                  "        transfer_13,\n"
                  "        transfer_14,\n"
                  "        selector_continuity,\n",
              ),
              (
                  '        "schema_version": "sergeant.static-status-review.v26",\n',
                  '        "schema_version": "sergeant.static-status-review.v27",\n',
              ),
              (
                  '        "static_transfer_13_review": transfer_13,\n'
                  '        "static_selector_continuity_review": selector_continuity,\n',
                  '        "static_transfer_13_review": transfer_13,\n'
                  '        "static_transfer_14_review": transfer_14,\n'
                  '        "static_selector_continuity_review": selector_continuity,\n',
              ),
          )

          for old, new in replacements:
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"expected one replacement target, found {count}: {old!r}")
              text = text.replace(old, new, 1)

          path.write_text(text, encoding="utf-8")
          PY

      - name: Run focused regression
        shell: bash
        run: |
          python -m pip install --quiet -e . pytest
          python -m pytest tests/test_static_transfer_14_review.py -q

      - name: Commit production correction and remove constructor
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add main_review/static_status_review.py
          git rm .github/workflows/temp-wire-transfer14.yml
          git commit -m "Wire transfer 14 officer into status review"
          git push origin HEAD:train/model-free-core-campaign-2
